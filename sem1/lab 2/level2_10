'''задание: 10. В группе учится и студентов. Каждый получил на экзаменах по 4 оценки. Подсчитать число студентов, не имеющих «2» и «3».'''

# Данные: оценки студентов
students_grades = [
    [4, 5, 4, 5],  # Пример оценок первого студента
    [3, 5, 4, 5],  # второго студента
    [5, 5, 5, 5],  # третьего студента
    [2, 5, 4, 5],  # четвёртого студента
    [5, 5, 5, 4]   # пятого студента
]
count_students = 0

# Перебираем каждого студента
for grades in students_grades:
    has_two = False  # Флаг наличия "2"
    has_three = False  # Флаг наличия "3"

    # Проверяем каждую оценку
    for grade in grades:
        if grade == 2:
            has_two = True
        if grade == 3:
            has_three = True

    # Если ни "2", ни "3" не найдено, увеличиваем счётчик
    if not has_two and not has_three:
        count_students += 1

# Вывод результата
print(f"Число студентов без оценок '2' и '3': {count_students}")

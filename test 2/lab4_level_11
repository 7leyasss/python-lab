11. Удалить строку матрицы А размером 5 * 7 содержащую мини- мальный элемент в 1-м столбце
```python
# Создаем матрицу размером 5x7
import numpy as np
A = np.random.randint(1, 100, size=(5, 7))  # Заполнение случайными числами от 1 до 100

# Находим минимальный элемент в первом столбце
min_element = A[:, 0].min()
min_index_row = A[:, 0].argmin()  # Индекс строки, содержащей минимальный элемент

# Удаляем строку с минимальным элементом в первом столбце
A = np.delete(A, min_index_row, axis=0)

# Выводим результат
print("Исходная матрица:")
print(A)
print("\nМатрица после удаления строки:")
print(A)
```

Этот код создает матрицу размером 5x7, заполняет её случайными числами от 1 до 100, находит минимальный элемент в первом столбце, удаляет строку, содержащую этот минимальный элемент, и выводит результат до и после удаления строки.

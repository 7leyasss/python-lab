19. В заданной матрице удалить все строки, содержащие нулевые элементы. Удаление строки оформить в виде метода.
Для решения этой задачи нужно создать метод, который будет проверять строки матрицы и удалять те, которые содержат хотя бы один нулевой элемент. Рассмотрим код, который выполняет эту задачу.

```python
def remove_zero_rows(matrix):
    # Фильтруем строки, проверяя, что в строке нет нулевых элементов
    return [row for row in matrix if 0 not in row]

# Пример использования
matrix = [
    [1, 2, 3],
    [0, 4, 5],
    [6, 7, 8],
    [9, 0, 10]
]

# Выводим матрицу до удаления
print("Матрица до удаления строк:")
for row in matrix:
    print(row)

# Применяем метод для удаления строк с нулями
matrix = remove_zero_rows(matrix)

# Выводим матрицу после удаления
print("\nМатрица после удаления строк с нулями:")
for row in matrix:
    print(row)
```

### Объяснение кода:
1. Метод `remove_zero_rows` принимает матрицу (список списков).
2. Для каждой строки в матрице мы проверяем, содержится ли в ней ноль с помощью условия `if 0 not in row`.
3. Все строки, которые не содержат нули, добавляются в новый список, который и возвращается.

### Пример:
Для матрицы:
```
[
    [1, 2, 3],
    [0, 4, 5],
    [6, 7, 8],
    [9, 0, 10]
]
```

Результат будет:
```
[
    [1, 2, 3],
    [6, 7, 8]
]
```

Матрицы, содержащие строки с нулями, будут отфильтрованы.

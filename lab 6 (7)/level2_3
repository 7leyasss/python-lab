3. На основании результатов соревнований по прыжкам в длину (фамилии и результаты трех попыток) составить итоговый протокол соревнований, считая, что в зачет идет лучший результат.

# Список участников и их результаты (фамилия, 3 попытки)
participants = [
    ("Иванов", 7.32, 7.45, 7.50),
    ("Петров", 7.18, 7.23, 7.40),
    ("Сидоров", 7.50, 7.60, 7.55),
    ("Кузнецов", 7.15, 7.20, 7.10),
    ("Михайлов", 7.30, 7.35, 7.25)
]

# Функция для вычисления лучшего результата из трех попыток
def best_result(participant):
    return max(participant[1], participant[2], participant[3])

# Сортируем участников по лучшему результату в порядке убывания
sorted_participants = sorted(participants, key=best_result, reverse=True)

# Выводим итоговый протокол с фамилиями и лучшими результатами
print("Итоговый протокол соревнований по прыжкам в длину:")
for i, participant in enumerate(sorted_participants, start=1):
    # Получаем лучший результат
    best_jump = best_result(participant)
    print(f"Место {i}: {participant[0]} — лучший результат {best_jump} м")

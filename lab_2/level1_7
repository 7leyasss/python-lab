# Ввод значения x
x = float(input("Введите значение x: "))

# Вычисление значения функции y
if abs(x) > 1:
    y = 1
else:
    y = abs(x)

# Вывод результата
print(f"Значение функции y при x = {x}: {y}")

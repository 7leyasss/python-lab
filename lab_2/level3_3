'''Условие: Ученику 1-го класса назначается дополнительно стакан молока (200 мл), если его вес составляет меньше 30 кг. Определить, сколько литров молока потребуется ежедневно для одного класса, состоящего из и учеников. После взвешивания вес каждого ученика вводится в компьютер
#Уровень 3= количество данных заранее (до начала выполнения программы) неизвестно python'''

# Инициализация переменных
total_milk = 0  # Общий объём молока в литрах

# Цикл для ввода данных
print("Введите вес учеников в килограммах (введите 'stop' для завершения):")
while True:
    input_data = input()  # Ввод данных
    if input_data.lower() == 'stop':  # Завершение ввода
        break
    try:
        weight = float(input_data)  # Преобразование ввода в число
        if weight < 30:  # Проверка веса
            total_milk += 0.2  # Добавляем 200 мл (0.2 литра)
    except ValueError:
        print("Некорректный ввод. Пожалуйста, введите число или 'stop'.")

# Вывод результата
print(f"Общее количество молока, необходимое для класса: {total_milk:.2f} литров")

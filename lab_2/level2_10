#задание: 10. В группе учится и студентов. Каждый получил на экзаменах по 4 оценки. Подсчитать число студентов, не имеющих «2» и «3».

# Инициализация переменных
n = int(input("Введите количество студентов: "))

# Подсчет студентов без оценок "2" и "3"
count_students = 0
for i in range(n):
    marks = []
    for j in range(4):
        mark = int(input(f"Введите оценку студента {i + 1} на экзамене {j + 1}: "))
        if mark not in [2, 3]:
            marks.append(mark)
    if len(marks) == 4:
        count_students += 1

# Вывод результата
print(f"Количество студентов без оценок '2' и '3': {count_students}")

